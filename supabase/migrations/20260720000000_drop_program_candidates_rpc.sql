-- =============================================================
-- 정부지원사업 추천이 "후보 RPC + 임계값 컷"에서 "전체 공고 브라우즈 +
-- 적합도 힌트"로 전환되어, 앱이 gov_program을 직접 조회한다.
-- 더 이상 호출되지 않는 후보 조회 RPC를 제거한다.
-- =============================================================

drop function if exists match_gov_program_candidates(text, text[], text[], date, int);
